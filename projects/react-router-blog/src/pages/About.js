import { Container } from '../components/styles/Container.styled';

const About = () => {
	return (
		<Container>
			<p style={{ marginTop: '1rem' }}>
				This is a blog application that is meant to be a training
				exercise.
			</p>
		</Container>
	);
};

export default About;
